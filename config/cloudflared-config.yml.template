# Cloudflare Tunnel Configuration
# Tunnel: glitch-ai (d602618b-0f68-4537-bbad-01577e1a29dc)
# Credentials: ~/.cloudflared/d602618b-0f68-4537-bbad-01577e1a29dc.json

tunnel: d602618b-0f68-4537-bbad-01577e1a29dc

ingress:
  # Route glitch.cothekdesigns.com through the auth proxy (handles both SPA and terminal)
  - hostname: glitch.cothekdesigns.com
    service: http://localhost:4100

  # Glitch Trader frontend
  - hostname: glitch-trader.cothekdesigns.com
    service: http://localhost:3000

  # Glitch Trader API
  - hostname: glitch-trader-api.cothekdesigns.com
    service: http://localhost:4120

  # Pi web UI - routed through its own auth-proxy instance (4103 -> 8787),
  # same .server-password as Glitch. No Cloudflare Zero Trust required.
  - hostname: pi.cothekdesigns.com
    service: http://localhost:4103

  # Catch-all: return 404 for any other hostname
  - service: http_status:404
